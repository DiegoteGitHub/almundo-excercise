package com.almundo.app;

/**
 * This enumeration represents the role of each employee
 */
public enum Role {
	
	/** The employee operator. */
	OPERATOR,
	
	/** The employee supervisor. */
	SUPERVISOR,
	
	/** The employee director. */
	DIRECTOR,
	
	/** Role for ON HOLD calls. */
	ON_HOLD;
}
